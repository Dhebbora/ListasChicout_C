#include <locale.h>
#include <stdio.h> 
#include <stdlib.h> 


//Tive que comentar pois estava dando erro 

// int bool verifica(char x[4], char y[4]){
// 	for(int i=0; i<=3; i++){
// 		int d =  3;
// 		if(x[i] != y[d-i]){
// 			return false;
// 		}
// 	}
// 	return true;
// }


int main() { 
	setlocale(LC_ALL, "Portuguese");


   char x[4]; 
   char y[4]; 
   
   printf("Informe a string: "); 
   gets(x); 
   
   printf("Informe a string: "); 
   gets(y); 
   
   if (verifica(x, y))
      printf("%s é da forma contraria de %s\n", x , y); 
   else 
      printf("%s não é da forma contraria de %s\n", x, y); 
   return 0; 
}